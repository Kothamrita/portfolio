# Kothamrita Chakraborty — Portfolio

Personal portfolio built with React, Vite, Tailwind CSS, Framer Motion, and Lucide React.

## Local development

```bash
npm install
npm run dev
```

## Production build

```bash
npm run build
```

Place the resume file at `public/resume.pdf` to enable the existing resume download buttons.
